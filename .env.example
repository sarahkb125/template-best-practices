# Optional: GitHub token for higher API rate limits (5000/hour instead of 60/hour)
GITHUB_TOKEN=

# Optional: Railway API token for fetching template metadata
RAILWAY_API_TOKEN=

# Server port (default: 3000)
PORT=3000
